#include<iostream>

using namespace std;

int find(int n[],int ans) 
{
	//¤G¤À·j 
}

int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	unsigned long long n,q;
	while(cin>>n)
	{
		int * m =new int [n];
		while(n--)
		{
			cin>>m[n];
		}
		
	} 
	return 0;
}
